# Required
AUTH_SECRET=replace-with-random-long-string
ACCESS_PASSWORD=replace-with-strong-password
# Optional secure hashed variant (overrides ACCESS_PASSWORD if provided)
# Generate with: node -e "console.log(require('bcryptjs').hashSync(process.argv[1], 10))" your-password
ACCESS_PASSWORD_BCRYPT=

# Vercel Blob token (set in Vercel Project Settings)
BLOB_READ_WRITE_TOKEN=
